import s from "./Footer.module.css"

const Footer = (params) => {
    return (
        <footer className={s.footer}>
            All right is reserved.
        </footer>
    )
}
export default Footer;